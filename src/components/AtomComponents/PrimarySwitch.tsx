import { Switch, SwitchProps } from '@material-ui/core';
import React from 'react';

const PrimarySwitch = (props: SwitchProps) => {
  return <Switch color="primary" {...props} />;
};
export default PrimarySwitch;
